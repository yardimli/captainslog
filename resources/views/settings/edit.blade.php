<x-app-layout>
    <x-slot name="header"><h1 class="text-xl font-bold">Settings</h1></x-slot>
    <div class="mx-auto max-w-2xl p-4 sm:p-6 lg:p-8"><section class="panel"><h2 class="text-lg font-bold">OpenRouter</h2><p class="mt-1 text-sm text-slate-500">Your key is encrypted in the database and never sent to the browser. It is only used by the server for your requests.</p><form method="POST" action="{{ route('settings.update') }}" class="mt-5 space-y-4">@csrf @method('PATCH')<div><label class="label">API key</label><input type="password" class="input" name="openrouter_api_key" autocomplete="off" placeholder="{{ $hasApiKey ? 'Key saved — enter a new value to replace it' : 'sk-or-v1-…' }}"></div>@if($hasApiKey)<label class="flex gap-2 text-sm text-rose-600"><input type="checkbox" name="remove_api_key" value="1">Remove the saved API key</label>@endif<button class="btn">Save settings</button></form><div class="mt-6 border-t border-slate-200 pt-4 text-sm dark:border-slate-800"><a class="text-indigo-600" href="{{ route('profile.edit') }}">Account profile & password →</a></div></section></div>
</x-app-layout>
